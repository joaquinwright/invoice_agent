[project]
name = "receipt-eval"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.112.0",
    "datasets>=5.0.0",
    "pillow>=12.2.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
